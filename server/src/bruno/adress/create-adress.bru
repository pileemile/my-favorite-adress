meta {
  name: Create address
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/api/addresses
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Maison",
    "description": "Adresse de test",
    "searchWord": "10 Downing Street, London"
  }
}
